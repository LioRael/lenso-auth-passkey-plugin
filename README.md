# Lenso Passkey Auth Plugin

Removable WebAuthn passkey authentication for Lenso. The implementation lives
on a feature branch until its contract and PostgreSQL behavior have passed the
repository gates.
